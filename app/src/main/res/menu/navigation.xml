<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/translate_navigation"
        android:icon="@drawable/translate_icon"
        android:title="@string/translate_icon"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/bookmarks_history_navigation"
        android:icon="@drawable/bookmarks_history_icon"
        android:title="@string/bookmarks_history_navigation"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/settings_navigation"
        android:icon="@drawable/settings_icon"
        android:title="@string/settings"
        app:showAsAction="ifRoom"/>

</menu>